GCDrawKit Style Grammar

literals
	numbers
	All numbers accept comma separated digits.
		integers
			GOOD
				1,234
				12345
			BAD
				12,34
		real
			GOOD
				1,234.567
				12345.0
			BAD
				12,34.9
		hex
		Hex digits range from 0..9A..F
		(currently syntactically accepted BUT stored as string)
		
	strings
	Strings are delimited by single or double quotes but they must be matching.
		GOOD
			'some string'
			"some other string's stuff"

	
symbols
Symbols must begin with a letter but otherwise can be any sequence of digits and letters.

Key-Value Pairs
Example:
	aKey: aValue
	
arrays
Examples:
#()
#(1)
#(1 2 3 "a string")

dictionaries
Examples:
(a: 1 b: "two" tri: 33)

functions
(symbol params)
Example:
	(sum 1 2 3 5)

expressions
(symbol key-value-params)
Example:
	(color r: 1.0 g: 1.0 b: 2.0 a: 1.0)

